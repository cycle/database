---

on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - master
      - '*.*'
      - '*.*.*'
  pull_request: null

name: MSSQL tests

jobs:
  phpunit:
    uses: cycle/gh-actions/.github/workflows/db-mssql.yml@master

...
